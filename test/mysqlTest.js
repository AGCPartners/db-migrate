var assert = require('assert');
var mysql = require('mysql');
var index = require('../index.js');

describe('When a mysql database is created', function() {

  it('i should get the mysql connection', function() {

  });
});